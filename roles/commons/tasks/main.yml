---
# Fact os vars
- name: Override config file directory for Debian
  when: ansible_os_family == "Debian"
  set_fact:
    env_config_dir: "/etc/default"

- name: Override service file directory for Debian
  when: ansible_os_family == "Debian"
  set_fact:
    service_dir: "/lib/systemd/system"
