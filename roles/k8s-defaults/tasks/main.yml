---

- name: Configure Kubernetes default vars
  debug:
    msg: "Check roles/k8s-defaults/defaults/main.yml"
  tags:
    - always

- name: Include vars of system-path.yml
  include_vars:
    file: defaults/system-path.yml
