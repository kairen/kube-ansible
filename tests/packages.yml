---

- hosts: test
  become: yes
  gather_facts: no
  roles:
    - { role: common/packages, pkg: "{{ downloads.etcd }}" }
    - { role: common/packages, pkg: "{{ downloads.etcd }}", create_link: true }
